# Repository events that start pipelines. The GitHub App delivers the events.
provider: github
team: platform
management: nopsai
triggers:
  - on: push
    branches:
      - main
    scope: dev
    pipelines:
      - platform/build-and-test

  - on: pull_request
    scope: dev
    pipelines:
      - platform/build-and-test
