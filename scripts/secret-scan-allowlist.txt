# Allowlist for scripts/secret-scan.sh.
#
# One extended regex per line, matched against "<path>:<line-number>:<matched line>".
# Only add entries for documentation placeholders and test fixtures that are
# provably not real credentials. Never allowlist a value that was ever live.

# Product documentation shows the shape of a GitHub App private key setting.
services/ui/src/features/product-docs/content/fields/environment\.ts:.*BEGIN RSA PRIVATE KEY-----\.\.\.

# Local development defaults that the production startup gates already reject.
docker-compose\.yaml:.*(changeme|localdev|development-only)

# Self-contained test fixtures generate throwaway keys at runtime.
.*_test\.go:.*
.*\.test\.tsx?:.*
